{
  "name": "memes-monorepo",
  "private": true,
  "scripts": {
    "build:client": "rm -rf ./dist && lerna run build --scope=@memes/client --stream"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
